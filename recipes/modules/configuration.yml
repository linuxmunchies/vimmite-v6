---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: files
    files:
      - source: vimmite
        destination: /

  - type: script
    scripts:
      - install-nerd-fonts.sh
      - install-zimfw.sh
      # After packages.yml installs btop. A later RPM replace would drop the cap.
      - enable-btop-perfmon.sh

  - type: justfiles

  # The Brew module installs Homebrew itself; this service declaratively
  # reconciles the small list of image-managed formulae after that setup.
  - type: systemd
    system:
      enabled:
        - vimmite-brew-packages.service
    user:
      enabled:
        - vimmite-first-login.service
